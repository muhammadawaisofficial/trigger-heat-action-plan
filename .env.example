# Copy to .env and add your key. .env is gitignored and must never be committed.
# TRIGGER runs fully offline from data/cache/ without this key.
FORTYGUARD_API_KEY=your_api_key_here
FORTYGUARD_BASE_URL=https://api.fortyguard.com
