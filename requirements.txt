# Core — everything needed to reproduce the headline number offline
requests>=2.31.0
python-dotenv>=1.0.0
shapely>=2.0.0          # area-weighted tile -> zone aggregation
pdfplumber>=0.10.0      # PDF text + page anchors for quote verification

# Tests — Phase 1 foundation suite (pytest tests/)
pytest>=8.0.0

# Interface
streamlit>=1.30.0
folium>=0.15.0
streamlit-folium>=0.18.0
pandas>=2.1.0

# Clause compiler — only needed to RE-compile the plan. The compiled output is
# committed, so reproducing our reported score needs no LLM key at all.
google-genai>=1.0.0     # default provider (free AI Studio tier)
# anthropic>=0.40.0     # optional alternative provider, see src/llm.py
